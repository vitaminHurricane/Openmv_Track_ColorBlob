#include "main.h"
#include "YunTai.h"
#include "Track.h"

void Track_Blob_Grad(uint8_t x, uint8_t y)
{
    int8_t err_x = MID_W - x;       //x轴，水平方向上的偏差
    int8_t err_y = MID_H - y;       //y轴，竖直方向上的偏差
    if (err_x > 0) {
        
    }
}

